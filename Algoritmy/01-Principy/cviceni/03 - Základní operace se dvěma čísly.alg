{ Základní operace se dvìma èísly }
zaèátek
  èti(x);
  èti(y);
  
  napiš(x, " + ", y, " = ", x + y);
  napiš(x, " - ", y, " = ", x - y);
  napiš(x, " * ", y, " = ", x * y);
  napiš(x, " / ", y, " = ", x / y);
  
  napiš(x, " div ", y, " = ", x div y);
  napiš(x, " mod ", y, " = ", x mod y);
konec